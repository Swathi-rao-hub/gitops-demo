apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: pre-reversewords
  name: pre-reversewords-route
spec:
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    name: pre-reversewords
  port:
    targetPort: 9001
